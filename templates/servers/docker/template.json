{
  "name": "docker",
  "url": "https://github.com/punkpeye/mcp-docker-server",
  "description": "Docker container and image management",
  "command": "npx",
  "args": [
    "-y",
    "mcp-docker-server"
  ],
  "env": {
  },
  "prerequisites": [
    {
      "binary": "npx",
      "check": "npx --version",
      "install": "npm install -g npx"
    }
  ],
  "recommends": [

  ],
  "alternatives": [

  ],
  "requires": [

  ],
  "healthCheck": {
    "type": "stdio",
    "timeout": "5s",
    "expectTools": [
      "docker_list_containers"
    ]
  }
}
